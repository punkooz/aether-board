# AetherBoard QA Proof — 2026-02-12

## Scope executed
- Backend unit/integration tests
- Backend build validation (TypeScript compile)
- API smoke + edge-case validation (auth, transitions, role gates)
- Visual UI capture across all core tabs

## Hard evidence

### 1) Backend automated tests
- Command: `npm test` (backend)
- Result: **PASS** (3/3 tests)

### 2) Backend build
- Command: `npm run build` (backend)
- Result: **PASS**

### 3) API scenario checks (manual deterministic)
- `GET /tasks` without auth => **401**
- `POST /tasks` as CEO => **201**
- invalid transition `todo -> done` => **400**
- valid transition `todo -> in-progress` => **200**
- `POST /tasks/:id/comments` => **201**
- `POST /rooms/:id/messages` => **201**
- `POST /milestones` => **201**
- `PATCH /tasks/:id` unauthenticated => **401**
- governor transition `review -> done` => **403** with role-gate error

Evidence snippets:
- `/tmp/q3.json`: `{"error":"invalid transition todo -> done"}`
- `/tmp/q4.json`: transitioned task payload with status `in-progress`
- `/tmp/q8.json`: `{"error":"Unauthorized. Provide x-role + x-user-id headers."}`
- `/tmp/q9.json`: `{"error":"Governors cannot transition directly to done."}`

### 4) Visual proof (captured screenshots)
Captured via Playwright script against running frontend:
- `frontend/screenshots/board.png`
- `frontend/screenshots/task.png`
- `frontend/screenshots/agents.png`
- `frontend/screenshots/rooms.png`
- `frontend/screenshots/milestones.png`
- `frontend/screenshots/reviews.png`

## Edge cases checked
- Missing auth headers
- Invalid status transition
- Governor role restriction to `done`
- Comments/messages/milestones create path
- Review queue rendering path (tasks in `review`)

## Issues found
1. **Docker local build verification still blocked** by host Docker socket permissions for `clawd` (`/var/run/docker.sock` access denied).
   - Impact: cannot prove local image build on this host yet.
   - Fix: add `clawd` to docker group + re-login.

2. **Visual E2E now enabled but still basic** (smoke-level screenshots). 
   - Impact: does not yet include full scripted user journeys with assertions per control.
   - Fix next: add Playwright test suite with assertions (create task, transition flow, comments flow, room message flow, review queue).
